# Összetettebb eseménykezelés - double click + eseményobjektum

- példa a `dblclick` használatára
- `eseményobjektum` elkérése: mi ez, miket tudunk rajta keresztül elérni, néhány példa
- `e.target`
- variációk az handler callback használatára
